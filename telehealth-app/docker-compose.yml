services:
  sanavo:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      DATABASE_URL: "file:/app/data/sanavo.db"
      NODE_ENV: production
    volumes:
      - sanavo_data:/app/data
    restart: unless-stopped

volumes:
  sanavo_data:
